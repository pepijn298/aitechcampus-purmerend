version: '3.8'
services:
  web:
    image: node:18
    working_dir: /app/frontend
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    command: sh -c "cd frontend && npm install --no-audit --no-fund && npm run dev"
